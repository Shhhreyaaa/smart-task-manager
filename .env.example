# Secret key for security session hashes and CSRF tokens
SECRET_KEY=your_secret_key_here

# Database location (defaults to instance/tasks.db inside app folder)
DATABASE_URL=sqlite:///instance/tasks.db

# Flask running mode: development, testing, or production
FLASK_ENV=development
